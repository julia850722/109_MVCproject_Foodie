@model IEnumerable<Foodie.Models.PurchaseOffer>

@{
    ViewBag.Title = "RequestIndex";
}

<h2>查詢餐廳</h2>

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>*@

@using (Ajax.BeginForm(new AjaxOptions() { Confirm = "Hello AJAX", UpdateTargetId = "ajaxdiv" }))
{
    @Html.AntiForgeryToken()


<div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <p>
                餐廳：@Html.TextBox("searchString")
            <input type="submit" value="篩選" class="btn btn-default" />
        </div>
    </div>
    
    @*<table class="table">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DeadLineTime)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.OfferedRestaurant)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.SellerName)
                </th>

                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.DeadLineTime)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.OfferedRestaurant)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.SellerName)
                    </td>
                    <td>
                        @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                        @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                        @Html.ActionLink("Delete", "Delete", new { id = item.Id }) |
                    </td>
                </tr>
            }

        </table>*@

}

<div id="ajaxdiv">
    @*@Html.ActionLink("Back to List", "Index")*@
    @Html.Action("Info")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

